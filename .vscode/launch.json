{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "request": "launch",
            "name": "Debug",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}/web"
        }
    ]
}